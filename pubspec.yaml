name: gps_sms_gateway
description: GPS Control EC - SMS Gateway
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.13.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.3
  flutter_background_service: ^5.0.5
  flutter_local_notifications: ^17.0.0
  permission_handler: ^11.3.1
  wakelock_plus: ^1.2.5
  flutter_phone_direct_caller: ^2.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
